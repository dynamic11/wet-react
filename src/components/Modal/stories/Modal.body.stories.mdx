import { ArgsTable, Canvas, Meta } from '@storybook/addon-docs';

import Modal from '../Modal';

<Meta title="Components/Modal/Body" component={Modal} />

export const TemplateGroup = ({ title, content, closeButton, ...rest }) => {
  return (
    <Modal.Dialog {...rest}>
      <Modal.Header closeButton closeLabel="goodbye">
        <Modal.Title> {title} </Modal.Title>
      </Modal.Header>
      <Modal.Body>{content} </Modal.Body>
    </Modal.Dialog>
  );
};

# Modal

<Canvas>
  <Story
    name="Body"
    args={{
      content: 'This is the location on the content of this Modal',
      title: 'success',
      closeButton: false,
      show: true,
      animation: true,
      restoreFocus: true,
      keyboard: true,
      centered: true,
    }}
  >
    {TemplateGroup.bind({})}
  </Story>
</Canvas>
