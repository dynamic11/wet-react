import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { Button } from '../..';

<Meta title="Components/Button/Active" component={Button} />

export const Template = (args) => <Button {...args} />;

# Button

## Active state

All styling variations in the `active` state:

<Canvas>
  <Story
    name="Default"
    args={{ active: true, children: 'Active primary button' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Primary"
    args={{
      active: true,
      variant: 'primary',
      children: 'Active primary button',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Danger"
    args={{ active: true, variant: 'danger', children: 'Active danger button' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Success"
    args={{
      active: true,
      variant: 'success',
      children: 'Active success button',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Warning"
    args={{
      active: true,
      variant: 'warning',
      children: 'Active warning button',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Info"
    args={{ active: true, variant: 'info', children: 'Active info button' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Link"
    args={{ active: true, variant: 'link', children: 'Active link button' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
