import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { Button } from '../..';

<Meta title="Components/Button/Disabled" component={Button} />

export const Template = (args) => <Button {...args} />;

# Button

## Disabled state

All styling variations in the `disabled` state:

<Canvas>
  <Story
    name="Default"
    args={{ disabled: true, children: 'Disabled primary button' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Primary"
    args={{
      disabled: true,
      variant: 'primary',
      children: 'Disabled primary button',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Danger"
    args={{
      disabled: true,
      variant: 'danger',
      children: 'Disabled danger button',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Success"
    args={{
      disabled: true,
      variant: 'success',
      children: 'Disabled success button',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Warning"
    args={{
      disabled: true,
      variant: 'warning',
      children: 'Disabled warning button',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Info"
    args={{ disabled: true, variant: 'info', children: 'Disabled info button' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Link"
    args={{ disabled: true, variant: 'link', children: 'Disabled link button' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
