import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { ProgressBar } from '../..';

<Meta title="Components/ProgressBar/Label" component={ProgressBar} />

export const Template = (args) => <ProgressBar {...args} />;

# ProgressBar

## ProgressBar label

Add a label prop to show a visible percentage. For low percentages, consider adding a min-width to ensure the label's text is fully visible.

<Canvas>
  <Story name="With Label" args={{ now: 60, label: '60%' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Screenreader only label

<Canvas>
  <Story
    name="Screenreader only label"
    args={{ now: 60, label: '60%', visuallyHidden: true }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
