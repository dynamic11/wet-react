import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { ProgressBar } from '../..';

<Meta title="Components/ProgressBar/Variants" component={ProgressBar} />

export const Template = (args) => <ProgressBar {...args} />;

# ProgressBar

## ProgressBar variants

Several variants are available for ProgressBar.

### default

<Canvas>
  <Story name="Default" args={{ now: 50 }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Success

<Canvas>
  <Story name="Success" args={{ variant: 'success', now: 50 }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Danger

<Canvas>
  <Story name="Danger" args={{ variant: 'danger', now: 50 }}>
    {Template.bind({})}
  </Story>
</Canvas>
