import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import ListGroup from '../ListGroup';

<Meta
  title="Components/List Group/Active"
  component={ListGroup}
  subcomponents={{
    'ListGroup.Item': ListGroup.Item,
  }}
/>

export const TemplateGroup = (args) => {
  return (
    <ListGroup numbered>
      <ListGroup.Item {...args}> {args.children} </ListGroup.Item>
    </ListGroup>
  );
};

# List Group Item Active

ListGroup items can have their `active` property toggled to highlight them. It should be set to true when the list group item has a link set to the current page.

<Canvas>
  <Story
    name="Active"
    args={{
      children: 'placeholder text',
      variant: 'info',
      disabled: false,
      action: true,
      href: 'https://www.youtube.com/',
      active: true,
    }}
  >
    {TemplateGroup.bind({})}
  </Story>
</Canvas>
