import { ArgsTable, Canvas, Meta } from '@storybook/addon-docs';

import ListGroup from '../ListGroup';

<Meta title="Components/List Group/Active" component={ListGroup} />

export const TemplateGroup = ({ children, ...rest }) => {
  return (
    <ListGroup numbered>
      <ListGroup.Item {...rest}> {children} </ListGroup.Item>
    </ListGroup>
  );
};

# List Group Item Active

ListGroup items can have their `active` property toggled to highlight them. It should be set to true when the list group item has a link set to the current page.

<Canvas>
  <Story
    name="Active"
    args={{
      children: 'placeholder text',
      variant: 'info',
      disabled: false,
      action: true,
      href: 'https://www.youtube.com/',
      active: true,
    }}
  >
    {TemplateGroup.bind({})}
  </Story>
</Canvas>
