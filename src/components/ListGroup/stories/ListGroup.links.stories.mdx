import { ArgsTable, Canvas, Meta } from '@storybook/addon-docs';

import ListGroup from '../ListGroup';

<Meta title="Components/List Group/Links" component={ListGroup} />

export const TemplateGroup = ({ children, ...rest }) => {
  return (
    <ListGroup numbered>
      <ListGroup.Item {...rest}> {children} </ListGroup.Item>
    </ListGroup>
  );
};

# List Group Item Links

ListGroup items can have a link attached to them using their `href` attribute to take users to a page on click if `action` is set to true.
If the item is disabled by setting the `disabled` attribute to true, clicking the item will do nothing.

<Canvas>
  <Story
    name="Links"
    args={{
      children: 'placeholder text',
      variant: 'success',
      disabled: false,
      action: true,
      href: 'https://www.youtube.com/',
    }}
  >
    {TemplateGroup.bind({})}
  </Story>
</Canvas>
