import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import ListGroup from '../ListGroup';

<Meta
  title="Components/List Group/Variants"
  component={ListGroup}
  subcomponents={{
    'ListGroup.Item': ListGroup.Item,
  }}
/>

export const Template = (args) => (
  <ListGroup>
    <ListGroup.Item {...args}> {args.text} </ListGroup.Item>
  </ListGroup>
);

# List Group

## List Group Item Variants

ListGroup items have several possible variants that change their apperance from their default.

<Canvas>
  <Story
    name="Info"
    args={{
      variant: 'info',
      text: 'Placeholder Text',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Warning"
    args={{
      variant: 'warning',
      text: 'Placeholder Text',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Success"
    args={{
      variant: 'success',
      text: 'Placeholder Text',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Danger"
    args={{
      variant: 'danger',
      text: 'Placeholder Text',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
