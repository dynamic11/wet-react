import { ArgsTable, Canvas, Meta } from '@storybook/addon-docs';

import ListGroup from '../ListGroup';

<Meta title="Components/List Group/Variants" component={ListGroup} />

export const TemplateGroup = ({ children, ...rest }) => {
  return (
    <ListGroup>
      <ListGroup.Item {...rest}> {children} </ListGroup.Item>
    </ListGroup>
  );
};

# List Group Item Variants

ListGroup items have several possible variants that change their apperance from their default.

<Canvas>
  <Story
    name="Info Variant"
    args={{
      children: 'placeholder text',
      variant: 'info',
    }}
  >
    {TemplateGroup.bind({})}
  </Story>
  <Story
    name="Warning Variant"
    args={{
      children: 'placeholder text',
      variant: 'warning',
    }}
  >
    {TemplateGroup.bind({})}
  </Story>
  <Story
    name="Success Variant"
    args={{
      children: 'placeholder text',
      variant: 'success',
    }}
  >
    {TemplateGroup.bind({})}
  </Story>
  <Story
    name="Danger Variant"
    args={{
      children: 'placeholder text',
      variant: 'danger',
    }}
  >
    {TemplateGroup.bind({})}
  </Story>
</Canvas>
